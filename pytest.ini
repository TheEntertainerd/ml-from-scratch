[pytest]
addopts = -v --maxfail=10 --cov=src --cov-report=term-missing
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')